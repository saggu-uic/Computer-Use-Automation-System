"""FakeBank Core: a deliberately legacy-style core banking app used as Rote's target.

Frames, table layouts, unlabelled inputs, no test IDs, CGI-style routes, and a
fault-injection control API on a separate port. All data is fake.
"""
